#include "main.h"

/**
 * _puts - A function that prints a string
 * @str: The string to be printed
 * Return: Nothing for now
 */

void _puts(char *str)
{
	char str;
	for (str[n] = str[0]; str[n] != '\0'; str[n]++)
	{
		_putchar((str[n]) + '0');
	}
}
