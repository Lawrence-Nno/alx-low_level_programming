#include <stdio.h>

/**
 * main - This program prints lowercase alphabets in reverse
 *
 * Return: And returns 0
 */
int main(void)
{
	char p;

	for (p = 'z'; p <= 'a'; a--)
		putchar(p);
	putchar('\n');
	return (0);
}
