#include <stdio.h>

/**
 * main - This is the main function
 *
 * No return for this proggram
 */

int main(void)
{
	char x;
	char Y;

	for (x = 'a'; x <= 'z'; x++)
		putchar(x);
	for (Y = 'A'; Y <= 'Z'; Y++)
		putchar(Y);
	putchar('\n')
}
