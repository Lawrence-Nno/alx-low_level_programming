#include "main.h"

/**
 * _abs - This functions computes the absolute value of a given number
 *
 * Return: Returns 0
 */

int _abs(int)
{
	int num;
	int x = (num * -1);
	if (num > 0)
		return (num);
	if (num == 0)
		return (0);
	if (num < 0)
		return (x);
}
