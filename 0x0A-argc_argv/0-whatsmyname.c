#include <stdio.h>

/**
 * main - This program prints its name
 * @argc: The argument count
 * @argv: The array of char holding the command line arguments
 * Return: Returns 0 on completion
 */

int main(int __attribute__((__unused__))argc, char* argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
